<%= form_with(model:[ @movie,comment]) do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this command from being saved:</h2>

      <ul>
        <% comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%#= form.label :comment %>
    <%= form.text_area :comment,placeholder:"Add a comment"%>
    <%= form.submit :"Send➤",class:"btn btn-outline-primary my-2 my-sm-0"%>
  </div>

<% end %>
